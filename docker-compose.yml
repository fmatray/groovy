version: '3'

services:
  db:
    image: postgres
  groovy:
    build: .
    command: python3 manage.py runserver --verbosity 3  0.0.0.0:8000
    volumes:
      - .:/groovy
    ports:
      - "8000:8000"
    depends_on:
      - db